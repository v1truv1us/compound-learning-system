# Compound Learning System Configuration
# Copy this file to ~/.config/compound-learning/config.env and customize

# Discord webhook URL for notifications (leave empty to disable)
# Get this from: Discord Server → Settings → Integrations → Webhooks
DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/YOUR_ID/YOUR_TOKEN"

# Claude model to use
CLAUDE_MODEL="claude-opus-4-5"

# OpenCode model to use
OPENCODE_MODEL="opencode-default"

# Timeout for Claude commands (seconds)
CLAUDE_TIMEOUT=600

# Timeout for OpenCode commands (seconds)
OPENCODE_TIMEOUT=600

# Root directory where projects are stored
GIT_ROOT="$HOME/git"
